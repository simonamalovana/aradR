% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reshape.R
\name{arad_wide}
\alias{arad_wide}
\title{Reshape ARAD data to wide format}
\usage{
arad_wide(
  data,
  snapshot = c("auto", "include", "ignore"),
  names_sep = "__"
)
}
\arguments{
\item{data}{A data frame with \code{indicator_id}, \code{period}, and \code{value} columns, typically returned by \code{arad_get()}.}

\item{snapshot}{How snapshot identifiers should affect column names: \code{"auto"} adds them only when an indicator has multiple snapshot contexts, \code{"include"} always adds them, and \code{"ignore"} never adds them.}

\item{names_sep}{Separator used between indicator and snapshot identifiers.}
}
\value{
A tibble with one \code{period} column and one numeric column per series. The \code{arad_diagnostics} attribute is preserved when present on \code{data}.
}
\description{
Converts the long-format output of \code{arad_get()} into one row per period and one column per series. Snapshot identifiers are incorporated into column names automatically when they are needed to distinguish multiple snapshot contexts for the same indicator.
}
